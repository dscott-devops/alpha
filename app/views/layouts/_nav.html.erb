<nav class="navbar navbar-expand-lg navbar-light bg-light">
<div class="container-fluid">
  <!--
<a class="navbar-brand" id="logo" href="<%= root_path %>">Alpha Blog</a>
-->
<%= link_to "Alpha Blog", root_path, class: "navbar-brand", id: "logo" %>
<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
  <span class="navbar-toggler-icon"></span>
</button>
<div class="collapse navbar-collapse" id="navbarSupportedContent">
  <ul class="navbar-nav me-auto mb-2 mb-lg-0">
    <!--
    <li class="nav-item">

      <a class="nav-link active" aria-current="page" href="<%= root_path %>"><i class="fas fa-home"></i> Home</a>
    </li>
    -->
    <li class="nav-item">
      <a class="nav-link active" aria-current="page" href="<%= users_path %>"><i class="fas fa-users-cog"></i> Users</a>
    </li>

    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
      <i class="fas fa-blog"></i> Articles</a>
      <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
        <li>
            <%= link_to "View All Articles", articles_path, class: "dropdown-item" %>
        </li>
        <li>
            <%= link_to "View All Categories", categories_path, class: "dropdown-item" %>
        </li>


        <% if logged_in? %>
        <li><a class="dropdown-item" href="<%= new_article_path %>">Create New Article</a></li>
  <% end %>
  <li>
      <%= link_to "View All Categories", categories_path, class: "dropdown-item" %>
  </li>
  <% if logged_in? && current_user.admin? %>
  <li>
      <%= link_to "Create New Category", new_category_path, class: "dropdown-item" %>
  </li>
    <% end %>

      </ul>
    </li>

    <!-- Chatroom Drop down -->
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <i class="fas fa-bomb"></i> Chatroom</a>
      <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
        <li>
            <%= link_to "Chatroom", chatroom_path, class: "dropdown-item" %>
        </li>
        <li>
            <%= link_to "Chat1", chat1_path, class: "dropdown-item" %>
        </li>
        <li>
            <%= link_to "Chat2", chat2_path, class: "dropdown-item" %>
        </li>
        <li>
            <%= link_to "View All Categories", categories_path, class: "dropdown-item" %>
        </li>


        <% if logged_in? %>
        <li><a class="dropdown-item" href="<%= new_article_path %>">Create New Article</a></li>
  <% end %>
  <li>
      <%= link_to "View All Categories", categories_path, class: "dropdown-item" %>
  </li>
  <% if logged_in? && current_user.admin? %>
  <li>
      <%= link_to "Create New Category", new_category_path, class: "dropdown-item" %>
  </li>
    <% end %>

      </ul>
    </li>
    <!--
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
      Categories
      </a>
      <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
        <li>
            <%= link_to "View All Categories", categories_path, class: "dropdown-item" %>
        </li>
        <% if logged_in? && current_user.admin? %>
        <li>
            <%= link_to "Create New Category", new_category_path, class: "dropdown-item" %>
        </li>
          <% end %>

      </ul>
    </li>
    -->

    <% if logged_in? %>

    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <%= "(Admin)" if current_user.admin? %> <%="#{current_user.first_name} #{current_user.last_name}" %>
      </a>
      <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
        <li>
            <%= link_to "View Profile", user_path(current_user), class: "dropdown-item" %>
        </li>
        <% if logged_in? %>
        <li><a class="dropdown-item" href="<%= edit_user_path(current_user) %>">Edit Profile</a></li>
        <li class="dropdown-item">
        <%= link_to "Log out", logout_path, class: "text-primary", method: :delete %>
        </li>

        <li><hr class="dropdown-divider"></li>
        <li>
          <%= link_to "Delete Profile", user_path(current_user), class: "dropdown-item text-danger", method: :delete, data: { confirm: "Are you sure you want to delete your own account"} %>
      </li>
        <% end %>
      </ul>
    </li>
    <% else %>
    <li class="nav-item">
    <%= link_to "Log in", login_path, class: "nav-link" %>
    </li>
    <li class="nav-item">
    <%= link_to "Sign Up", signup_path, class: "nav-link" %>
    </li>
    <% end %>

  </ul>
  <form class="d-flex">
    <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
    <button class="btn btn-outline-success" type="submit">Search</button>
  </form>
</div>
</div>
</nav>
